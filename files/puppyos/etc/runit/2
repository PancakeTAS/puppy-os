#!/bin/sh

PATH=/usr/bin

# switch runlevel
runsvchdir default

# launch the actual runlevel
ln -s /etc/runit/runsvdir/current /run/runit/service
exec env - PATH=$PATH runsvdir -P /run/runit/service 'log: ...........................................................................................................................................................................................................................................................................................................................................................................................................'
